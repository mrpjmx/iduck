# Pages placed under these sections will be shown on homepage and archive page.
mainSections = ["post"]
# Output page's full content in RSS.
rssFullContent = true
favicon        = "img/favicon.ico"

# Custom JS for Netlify Identity
customJS = ["/js/netlify-identity.js"]

# Accepted values: "default", "lastmod"
SortBy              = "default"

[footer]
    since      = 2026
    customText = "理想四旬"

[dateFormat]
    published   = "2006-01-02"
    lastUpdated = "2006-01-02"

[sidebar]
    emoji    = "🦆"
    subtitle = "Chuan`s memory"
    avatar   = "img/avatar.png"

[article]
    headingAnchor = false
    math          = false
    readingTime   = true

    [article.license]
        enabled = false

[widgets]
    homepage = [
        { type = "search" },
        { type = "archives", params = { limit = 5 } },
        { type = "categories", params = { limit = 10 } },
        { type = "tag-cloud", params = { limit = 10 } },
    ]
    page = [{ type = "toc" }]

[opengraph.twitter]
    site = ""
    card = "summary_large_image"

[colorScheme]
    toggle  = true
    default = "auto"

## Comments
[comments]
    enabled = true
    provider = "artalk"

[comments.artalk]
    serverUrl = "https://artalk.iduck.top/"
    site = "iduck"
